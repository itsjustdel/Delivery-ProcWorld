using UnityEngine;
using System.Collections;

public class PlaceAndRotate : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//Scan for terrain and put house on top using only the Y coordinate
	//	RaycastHit hit;

	
		//LayerMask myLayerMask = LayerMask.GetMask("Road");

	//	if (Physics.Raycast(transform.position, -Vector3.up, out hit, 1000.0F))//,myLayerMask))
	//		transform.position = hit.point;
	//	transform.position += Vector3.up* (transform.localScale.y *0.5f);

		//transform.Rotate(0f,Random.Range(0.0f, 360.0f),0f);

	}
	

}
